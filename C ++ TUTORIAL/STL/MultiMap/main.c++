/*
    NOTE: Duplicate keys are aallowed and Everything in sorted order

    Everything same as only are stored multiple kkeys only app[key]
    used here.


*/

int main(){
    //same as set and other Map properties.
    return 0;
    
}