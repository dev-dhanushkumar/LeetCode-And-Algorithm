#include<bits/stdc++.h>
using namespace std;

void explainUnOrderedSet() {
    unordered_set<int> st;
    /*
        Lower and Upper bound doesn't work because of it couldn't store the sorted
        values in set only stores the Unique elements only.
        After that all functions are same as above it does not stored in any 
        particular order. it has a better complexity.

        Complexity -> O(1)
    */

}

int main() {
    explainUnOrderedSet();
    return 0;
}